/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'images.unsplash.com',
            'images.pexels.com', 'img-new.cgtrader.com', 'wallpapercave.com'],
    }
};

export default nextConfig;
